import { Metadata } from "next";
import PageContent from "./PageContent";

export const metadata: Metadata = {
  title: "Find node excentriciy of custom graph | Graph Theory ",
  description:
    "Create a graph using the graph editor and find the node excentricity of any node in the graph. With detail step by step explanation and algorithms details",
};

export default function BruteForceMinimumNodeColoringForm() {
  return <PageContent />;
}
