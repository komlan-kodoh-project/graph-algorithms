import { Metadata } from "next";
import PageContent from "./PageContent";

export const metadata: Metadata = {
  title: "Find Maximum Independent Set of Custom Graph | Graph Theory ",
  description:
    "Create a graph using the graph editor and find the maximum independent sets in the graph. With detail step by step explanation and algorithms details",
};

export default function BruteForceMinimumNodeColoringForm() {
  return <PageContent />;
}
