import { Metadata } from "next";
import PageContent from "./PageContent";

export const metadata: Metadata = {
  title: "Find Shortest Path in Custom Graph | Dijkstra",
  description:
    "Create a graph using the graph editor and find the shortest path from two point in the graph using the Dijkstra algorithm. With detail step by step explanation and algorithms details.",
};

export default function BruteForceMinimumNodeColoringForm() {
  return <PageContent />;
}
