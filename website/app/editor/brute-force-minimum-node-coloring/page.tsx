import { Metadata } from "next";
import PageContent from "./PageContent";


export const metadata: Metadata = {
  title: "Find Minimum Node Coloring in Custom Graph | Brute Force ",
  description:
    "Create a graph using the graph editor and find the minimum node coloring using a brute force algorithm. With detail step by step explanation and algorithms details.",
};

export default function BruteForceMinimumNodeColoringForm() {
  return <PageContent />;
}
