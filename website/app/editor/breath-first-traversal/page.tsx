import { Metadata } from "next";
import PageContent from "./PageContent";

export const metadata: Metadata = {
  title: "Breath First Traversal of Custom Graph Online | BFS",
  description:
    "Create a graph using the graph editor and watch a colorful traversal using breath first search. With detail step by step explanation and algorithms details.",
};

export default function BruteForceMinimumNodeColoringForm() {
  return <PageContent />;
}
