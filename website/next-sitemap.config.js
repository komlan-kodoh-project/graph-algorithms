/** @type {import('next-sitemap').IConfig} */
// Default code you can customize according to your requirements.
module.exports = {
  siteUrl: process.env.SITE_URL || "https://graph.komlankodoh.com",
  generateRobotsTxt: true, // (optional)
  // REST CODE READ DOCS  ...
};
